import MessageTransceiver from './messageTransceiver'

describe('MessageTransceiver testings ...', () => {
  it('Send to running', () => void 0)

  it('Listen', () => void 0)

  it('Send to tab', () => void 0)

  it('Send to all tabs', () => void 0)
})
