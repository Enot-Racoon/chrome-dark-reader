// Storage key in chrome extensions store
export const STORAGE_KEY = '--@chrome-dark-reader'

// Relative path in public directory
export const ICON_DIR = '/static/icons'

export default {
  STORAGE_KEY,
  ICON_DIR,
}
